apiVersion: v1
kind: Service



metadata:
  name: url-shortner-svc

spec:
  type: NodePort
  selector:
    app: url-shortener-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30007

